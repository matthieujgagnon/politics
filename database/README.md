# /database

Migrations, seed data, and entity-relationship docs.

Implements the data model and schema defined in [`PROJECT.md`](../PROJECT.md) §8 (Data model), §9 (Entity relationships), and §18 (Database schema).

Starts empty. Phase 1 (§27) adds the migration tooling and the initial Phase 1–2 tables from §18's logical schema — not all entities in §8, only the ones the current phase needs.
